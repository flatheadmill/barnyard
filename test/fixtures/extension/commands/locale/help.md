# desc
Converge the system locale.
# opt help
Display help for `barnyard locale`.
# man
## DESCRIPTION
`barnyard locale` converges the machine's system locale. The language is
read from the module configuration, not the command line.
## OPTIONS
> options
## COMMANDS
> commands
